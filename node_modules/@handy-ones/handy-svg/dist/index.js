var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { useRef, useEffect } from 'react';
import { injector } from './lib/injector';
const HandySvg = (props) => {
    const { src, loadTimeot, loadRetryCount } = props, restProps = __rest(props, ["src", "loadTimeot", "loadRetryCount"]);
    const isFirstRun = useRef(true);
    useEffect(() => {
        if (isFirstRun.current) {
            injector.load(src, { timeout: loadTimeot, retryCount: loadRetryCount }).catch(() => ({}));
            isFirstRun.current = false;
            return;
        }
        injector.load(src, { timeout: loadTimeot, retryCount: loadRetryCount, flushImmediate: true }).catch(() => ({}));
    }, [src]);
    return (_jsx("svg", Object.assign({}, restProps, { children: _jsx("use", { href: `#${injector.getId(src)}` }) })));
};
HandySvg.displayName = 'HandySvg';
export { injector, HandySvg };
